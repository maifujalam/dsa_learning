1. Expand the Block storage:-
  a. Expand the block device through UI
  b. Resize the partition: growpart /dev/nvme1n1  [ nvme1n1: is the disk bo the partition]
  c. Partition is increased not the file system: df -hT
  c. Resize the fs: resize2fs /dev/nvme1n1p1 [ nvme1n1p1: its the partition]
2. EBS only expand and not shrink.
3. EBS is of 2 types system and data disk.
4. OS disk can only be detached in stop state.
5. 